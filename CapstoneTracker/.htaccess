RewriteEngine On
RewriteBase /thesis-repository/

# Redirect all requests to index.php unless it's a real file or directory
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]